/**
 * Task Tests.
 *
 * Nothing in this file offers any functionality,
 * only tests of it.
 */
window.onload = () => {
  /**
   * Test for #1.1 task.
   */
  const ps = selectDOM('p');
  console.log(ps);
  console.log(ps.get(1));
};
